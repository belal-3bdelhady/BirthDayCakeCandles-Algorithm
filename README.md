# BirthDayCakeCandles-Algorithm
This recursive algorithm finds the tallest birthday cake candles and counts how many times they appear. It reduces the problem size by one in each step, updating the maximum height and its frequency count as the recursion unwinds.
